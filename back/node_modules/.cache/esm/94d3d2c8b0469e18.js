_b04‍.x([["default",()=>_b04‍.o]]);const mongoose = require("mongoose");
const { Schema } = mongoose.Schema;

const UserSchema = new Schema(
  {
    email: {
      type: String,
      require: "Email addres is required",
      unique: true
    },
    first_name: {
      type: String,
      require: "First name i required"
    },
    last_name: {
      type: String,
      require: "Last name is required"
    },
    username: {
      type: String,
      required: "Username is required"
    },
    password: {
      type: String
    },
    confirmed: {
      type: Boolean,
      default: false
    },
    avatar: String,
    confirm_hash: String,
    last_seen: {
      type: Date,
      default: new Date()
    }
  },
  { timestamps: true }
);

const UserModel = mongoose.model("User", UserSchema);

_b04‍.d(UserModel);
