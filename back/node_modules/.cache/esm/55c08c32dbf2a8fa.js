let express,createServer,mongoose,UserCtrl;_dbf‍.w("express",[["default",["express"],function(v){express=v}]]);_dbf‍.w("http",[["createServer",["createServer"],function(v){createServer=v}]]);_dbf‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_dbf‍.w("./controllers",[["UserCtrl",["UserCtrl"],function(v){UserCtrl=v}]]);






const DB = "tictactoe";
mongoose.connect(`mongodb://localhost:27017/${DB}`, {
  useNewUrlParser: true,
  useCreateIndex: true,
  useFindAndModify: false,
  useUnifiedTopology: true
});

const app = express();
const http = createServer(app);

// const UserController = new UserCtrl();

// app.post("/user/signup", UserController.creare);
// app.get("/user/:id", UserController.show);

const PORT = 3003;

http.listen(PORT, function() {
  console.log(`Server: http://localhost:${PORT}`);
});
